%article
	%header
		%h1 New user
	%section
		%p
			= semantic_form_for @user do |f|
				=f.input :name
				=f.input :email
				=f.input :password
				=f.input :password_confirmation, :label => "confirmation"
				=f.actions :submit

